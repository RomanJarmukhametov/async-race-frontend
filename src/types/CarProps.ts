/* This TypeScript code defines an interface named `CarProps` that specifies the structure of an object
representing a car. The interface includes three properties: `id` of type number, `name` of type
string, and `color` of type string. */
interface CarProps {
  id: number;
  name: string;
  color: string;
  raceStarted: boolean;
}

export default CarProps;
